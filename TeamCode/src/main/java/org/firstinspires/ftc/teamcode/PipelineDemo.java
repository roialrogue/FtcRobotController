/*package org.firstinspires.ftc.teamcode;

import org.opencv.core.Mat;
import org.opencv.core.Rect;
import org.opencv.core.Scalar;
import org.opencv.imgproc.Imgproc;
import org.openftc.easyopencv.OpenCvPipeline;

public class PipelineDemo extends OpenCvPipeline {

    private Mat workingMatrix = new Mat();
    public String position = "left";

    public static int matLrewStart = 0;
    public static int getMatLrewEnd = 100;

    public static int matLcolSart = 0;

    public static int matLcolEnd = 0;

    public double leftBlue = 0;
    //public pieplineDemo(){

    }
    public final Mat processFrame(Mat input) {

        input.copyTo(workingMatrix);

        if(workingMatrix.empty()) {
            return input;
        }*/

        /*Mat matL = workingMatrix.submat(matLrewStart,matLcolEnd,matLcolSart, matLcolEnd);
        Imgproc.rectangle(workingMatrix, new Rect(matLrewStart,matLcolSart,(matLcolEnd-matLcolEnd),(matLcolEnd-matLcolSart)), new Scalar(0,255,0));

        leftBlue = Core.sumElems(matL).val[0];
        leftBlue /=matL.rows() * matL.cols();

        if (leftBlue)*/

